
// Create array in data.json enabled to be access as a module or array that represent current module
const messages = require('./data/data.json')

// Make it to be accesed in data.js as a object
module.exports = {
    messages
}

